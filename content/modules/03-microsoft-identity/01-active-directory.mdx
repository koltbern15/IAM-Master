{/* Objectives current as of 2026-05-27. Re-verify each before a cert sitting. */}

<HoloPanel label="COVERS">
- **SC-300** Implement an identity management solution — hybrid identity foundations (AD DS as the on-prem source-of-truth synced by Entra Connect)
- **Security+ SY0-701** Objective 4.6 — Directory services
- **CISSP** Domain 5 (Identity and Access Management) — implement and manage authorization mechanisms (AD DS as the canonical enterprise example)
</HoloPanel>

# Active Directory Domain Services <SC300Badge />

Microsoft shipped Active Directory Domain Services in 1999 alongside Windows 2000 Server. Twenty-seven years later it still authenticates logins at the majority of enterprise organizations on earth. It is brittle, attack-surface-rich, 25-year-old software running mission-critical workloads — and you absolutely must understand it before anything else in this curriculum makes sense.

## What it is

AD DS is Microsoft's on-premises <Definition term="LDAP directory">A directory service built on the Lightweight Directory Access Protocol (RFC 4511). An LDAP directory is a hierarchical, attribute-based store optimized for read-heavy identity lookups. AD DS exposes LDAP on port 389 (or 636 for LDAPS) and stores every user, computer, group, and policy object in a replicated database called the NTDS.DIT.</Definition> combined with a Kerberos authentication service (RFC 4120), a DNS resolver, and a Group Policy engine. Those four components are deeply entangled — AD DS does not work without DNS, Kerberos does not work without AD DS, and Group Policy does not work without either.

The logical structure is a strict hierarchy: <Definition term="Forest">The outermost boundary of an Active Directory deployment. A forest contains one or more domains that share a single schema and a single global catalog. The forest is also the security boundary — a compromise of any domain in a forest must be treated as a compromise of all domains in the forest. This is the architectural fact that makes cross-forest trusts a strategic risk decision, not a technical one.</Definition> at the top, domains below it, and Organizational Units (OUs) inside domains. The schema — the master template that defines every object class and attribute — is forest-wide and replicated to every domain controller. This is a hard constraint: you cannot have a per-domain schema. If your forest has five domains and you extend the schema to support a custom application attribute, all five domains absorb that extension whether they want it or not.

<EcosystemMap />

The diagram above shows AD DS as the "directory" node (gray), positioned on the left. Every edge connecting it to other nodes — Entra ID, CyberArk, BeyondTrust, SailPoint — represents a dependency or integration. The edge between AD DS and Entra ID is the one this entire module lives on: AAD Connect (now called Microsoft Entra Connect) is the synchronization engine that makes that edge real.

## Why it exists

In 1999, the enterprise identity problem was straightforward in scope if not in implementation: how do you let one Windows domain account log into any machine in the building and access any file share it is authorized to reach? The answer was Kerberos — a ticket-granting protocol designed at MIT (RFC 1510, now superseded by RFC 4120) — layered on top of an LDAP directory that stored the accounts, and glued together by DNS so that clients could find domain controllers automatically via SRV records.

This was genuinely elegant for its era. The Kerberos model has a clean separation between authentication (proving who you are to the Key Distribution Center) and authorization (presenting a service ticket to a resource). Kerberos tickets are short-lived and cryptographically bound. Group Policy provided the first serious enterprise Configuration-as-Code: one XML-like document, linked to an OU, and every machine in that OU gets the security baseline applied automatically on next refresh. For 2000, this was remarkable.

The problem is that the world changed and AD DS did not. The protocol trust model that was elegant in a sealed office LAN is dangerous when laptops leave the building, when service accounts accumulate privileges over decades because nobody documented what they did, and when a single domain controller compromise gives an attacker Domain Admin over the entire forest.

<ProTip>
Microsoft's AD DS engineering team is in maintenance mode. Security patches ship; new capabilities do not. Every roadmap document Microsoft publishes steers you toward Entra ID for modern identity workloads. This does not mean you can ignore AD DS — most organizations will run it for another decade — but you should not be making architectural bets that depend on AD DS becoming more capable. Assume it will remain exactly as dangerous as it is today.
</ProTip>

## How it works under the hood

**The replication topology.** AD DS replicates via a multi-master model within a domain: any writable domain controller can accept a change, and the Knowledge Consistency Checker (KCC) automatically builds a replication topology between DCs using site and subnet objects you define. Sites and subnets are AD DS's model of your physical network — you tell AD DS which IP ranges belong to which site, and AD DS uses that information to route client logon requests to a nearby DC and to control when inter-site replication happens (you can throttle it to off-hours across slow WAN links). Get your sites-and-subnets configuration wrong and clients in a remote branch are authenticating across a 10 Mbps MPLS link instead of the DC sitting in the server room next to them.

**FSMO roles.** Five operations in AD DS cannot be multi-master safely, so Microsoft designates a single DC for each as a Flexible Single Master Operation role. The five roles, their scope, and what breaks when they go offline:

- **Schema Master** (forest-wide): the only DC that can write schema changes. Going offline means you cannot extend the schema; normal operations are unaffected.
- **Domain Naming Master** (forest-wide): must be online to add or remove a domain from the forest. Normal operations unaffected when offline.
- **PDC Emulator** (per-domain): the most operationally critical role. It processes password changes preferentially (so a just-changed password works before replication completes), synchronizes the domain's time hierarchy (Kerberos tickets fail if clocks drift more than 5 minutes — the PDC Emulator is the authoritative time source), and processes Group Policy changes. A PDC Emulator outage produces intermittent authentication failures, account lockout storms, and GPO application failures within minutes in a busy environment.
- **RID Master** (per-domain): allocates pools of Relative Identifiers to other DCs. A DC without RIDs cannot create new objects. Outage is low-impact until RID pools exhaust — then object creation halts.
- **Infrastructure Master** (per-domain): maintains cross-domain group membership references. Low operational impact except in multi-domain forests where it produces stale membership data if offline for extended periods.

The practical implication: the PDC Emulator is your highest-availability FSMO requirement. In any serious environment it lives on a virtualized DC with a very aggressive recovery SLA.

**The protocol triad.** AD DS exposes three protocols everything else depends on: LDAP (directory queries and object management), Kerberos (authentication ticket exchange), and DNS (DC and service location). Remove any one of them and AD DS stops functioning. This also means that anyone with network access to your domain controllers has three distinct protocol attack surfaces pointed at them — which is why DC placement behind tight firewall rules and the Tier 0 isolation model are non-negotiable in hardened environments.

**Group Policy as Configuration-as-Code.** A Group Policy Object (GPO) is a collection of settings — registry values, script execution, software installation, security policy, audit configuration — linked to a site, domain, or OU and applied to every computer and user object in scope at next refresh. GPOs process in LSDOU order: Local, Site, Domain, OU (innermost OU wins for conflicts). This inheritance model is powerful and merciless. A single misconfigured GPO linked to the domain root affects every machine in the domain.

**Group scoping — the AGDLP model.** AD DS gives you three group scopes — Global, Domain Local, and Universal — and the scope is not cosmetic. It dictates which objects a group can contain and where the group can be granted permissions. Microsoft's prescriptive way to wire those scopes together is **AGDLP**: **A**ccounts go into **G**lobal groups, Global groups nest into **D**omain **L**ocal groups, and the Domain Local group is the one you grant **P**ermissions on the resource. You put *who* (users, by role or department) into Global groups, you put *what they can do* (the permission on a share, an OU, a printer) onto Domain Local groups, and you connect the two by nesting. The payoff is that membership changes (a new hire, a transfer) happen once, in the Global group, and access flows through automatically — you never touch the resource ACL again.

<Definition term="AGDLP">Accounts → Global → Domain Local → Permissions. Microsoft's canonical role-based access pattern in a single-domain AD DS environment. Accounts are placed in Global groups (which represent roles or org units and only contain objects from their own domain). Global groups nest into Domain Local groups (which can pull members from any trusted domain but only grant access on resources in their own domain). The Domain Local group is what appears on the resource's ACL. This indirection is the on-prem ancestor of every RBAC model you will meet later in this curriculum.</Definition>

In a multi-domain forest, the chain grows one link to **AGUDLP**: **A**ccounts → **G**lobal → **U**niversal → **D**omain **L**ocal → **P**ermissions. Universal groups (whose membership replicates to the global catalog, so it is visible forest-wide) sit between the per-domain Global groups and the per-domain Domain Local groups, letting you collect role groups from several domains into one forest-wide bucket before granting access.

<ProTip>
Why not just dump user accounts straight onto resource ACLs, or grant permissions directly to Global groups? Because it does not scale and it does not survive multi-domain growth. Direct ACLs mean every access change is a resource-by-resource ticket, and Global groups cannot be granted permissions on resources in *other* domains. AGDLP/AGUDLP is the discipline that keeps "who has access" (Global/Universal) cleanly separated from "what access exists" (Domain Local) — and on the exam, when you see a question about which group scope holds members versus which one goes on the ACL, the answer almost always traces back to this model. Watch the trap: Universal group membership replicates to the global catalog, so churning a Universal group's members triggers forest-wide GC replication — which is exactly why you nest *Global* groups into Universal groups rather than individual users.
</ProTip>

**The attack surface.** BloodHound is an open-source graph analysis tool that ingests data from an AD DS environment and maps every privilege escalation path from an arbitrary user account to Domain Admin. It was released in 2016 and has been in continuous production use by red teams and attackers ever since. The existence of BloodHound is the most honest indictment of AD DS's security model available: the attack paths it finds are not exotic bugs — they are the logical consequence of permission inheritance, nested group membership, delegation rights, and Kerberoastable service accounts accumulated over years of normal operations.

The canonical attack chain: Kerberoasting extracts a service ticket encrypted with a service account's password hash from the KDC (no special privileges required — any domain user can request a service ticket for any SPN). The attacker takes the ticket offline and cracks the hash. If the service account has elevated privileges or a path to elevated privileges in BloodHound, the attacker follows that path to Domain Admin. Average time from initial Kerberoasting to Domain Admin in an unhardened environment on a red team engagement: under an hour.

<Flashcard front="What are the five FSMO roles, their scope, and which one has the highest operational impact when offline?" back="Schema Master (forest-wide) — blocks schema changes only. Domain Naming Master (forest-wide) — blocks domain add/remove only. PDC Emulator (per-domain) — highest impact: drives password sync, time sync (Kerberos), and GPO processing. RID Master (per-domain) — blocks new object creation when pools exhaust. Infrastructure Master (per-domain) — degrades cross-domain group membership accuracy in multi-domain forests." />

<Flashcard front="What is the Microsoft AD DS tiering model and what is in Tier 0?" back="Microsoft's admin tiering model (Enterprise Access Model) separates administration into three tiers to contain lateral movement. Tier 0 = the identity control plane: domain controllers, FSMO role holders, AD DS admin accounts, privileged workstations (PAWs) used to manage Tier 0. A Tier 0 compromise is a forest compromise. Tier 1 = servers and services. Tier 2 = user workstations. Admin accounts must not be used across tiers." />

## War story

<WarStory title="The 45-Minute Path to Domain Admin">
A regional community bank, 38 branches, about 800 employees. The AD DS forest was 22 years old — it predated the bank's last two core-banking platform migrations and had absorbed two acquisitions. Nobody alive at the bank had been there when the forest was built.

During a red team engagement, the initial foothold was a phished branch teller — a credential harvested from a fake password-reset email. The account had no special privileges. Standard domain user. The red team ran SharpHound (the BloodHound data collector) from the compromised workstation. Forty-five minutes after the initial foothold, they had Domain Admin.

The path BloodHound mapped: the teller's account was a member of a distribution list that had been granted "GenericWrite" on an OU containing IT helpdesk computers (someone had done this to allow automated printer installs years ago, never documented, never reviewed). GenericWrite on a computer object lets you set the `msDS-AllowedToActOnBehalfOfOtherIdentity` attribute, which enables Resource-Based Constrained Delegation (RBCD) abuse. From there, the red team impersonated a Domain Admin to the helpdesk workstation, harvested credentials from memory via the compromised session, and walked into the domain.

None of the components were bugs. Every permission was "by design" — it was just design from a decade ago that nobody had reviewed. There was no Tier 0 boundary. Domain controllers were on the same VLAN as member servers. There was no PAW. There were 47 accounts with Domain Admin rights, 12 of which appeared to belong to former employees.

The remediation project ran 14 months. The hardest part was not technical — it was organizational. Every quarter, someone wanted to re-add their admin account to Domain Admins "just to fix something real quick." The security team built an approval workflow using PIM (via Entra ID, for the hybrid-synced accounts) so that Domain Admin elevation required a ticket, a second approver, and a 4-hour window. The workflow was resented and fought. It held.

In a SaaS-first organization running Entra-only, there is no forest to compromise — but the conceptual equivalent is an Entra ID Global Administrator account with no PIM, no MFA, and a permanent active assignment. The attack path is different; the organizational pattern is identical. In a defense environment, air-gapped AD DS forests at classification boundaries mean there is no Entra Connect sync — the attack surface is contained to the network, which is why physical and logical access control to domain controllers is treated as a Tier 0 perimeter problem. In healthcare, Epic's integration with AD DS creates a common exception pattern: "Epic needs this service account to be a Domain Admin because the vendor said so." It is almost never true. Challenge it every time.
</WarStory>

## Recall drill

<Flashcard front="In AD DS, what are the three levels of the logical hierarchy from outermost to innermost?" back="Forest (outermost — shares one schema and one global catalog, and is the security boundary), then Domain (a replication and administrative boundary inside the forest), then Organizational Unit (OU — a container inside a domain for grouping objects and linking Group Policy). Users, computers, and groups live inside OUs." />
<Flashcard front="Why is the forest — not the domain — the true security boundary in AD DS?" back="Because a single schema and global catalog are shared forest-wide, and trust relationships and replication paths mean a compromise of any domain in a forest must be treated as a compromise of all domains in that forest. The domain is only an administrative and replication boundary, not a security boundary." />
<Flashcard front="Which two FSMO roles are forest-wide, and which three are per-domain?" back="Forest-wide: Schema Master and Domain Naming Master (one of each per forest). Per-domain: PDC Emulator, RID Master, and Infrastructure Master (one of each per domain). So a single-domain forest has all five on potentially one DC; a three-domain forest has 2 forest-wide plus 3 x 3 per-domain roles." />
<Flashcard front="What three critical functions does the PDC Emulator FSMO role perform?" back="It processes password changes preferentially (so a just-changed password works before full replication), it is the authoritative time source for the domain (Kerberos fails if clocks drift more than 5 minutes), and it processes Group Policy changes. It is the highest-availability FSMO role because its outage causes auth failures and lockout storms quickly." />
<Flashcard front="What is the LSDOU order of Group Policy processing, and which link wins a conflict?" back="GPOs apply in the order Local, then Site, then Domain, then OU (LSDOU). Later-applied settings overwrite earlier ones, so the innermost OU wins conflicts by default. Block Inheritance and Enforced (no-override) links can alter this normal precedence." />
<Flashcard front="What does the AGDLP model stand for and what is the purpose of each link?" back="Accounts go into Global groups, Global groups nest into Domain Local groups, and Domain Local groups receive Permissions on the resource. Global groups represent who (roles or org units); Domain Local groups represent what access exists (the ACL entry). The nesting separates membership churn from resource ACLs so a new hire is added once in the Global group." />
<Flashcard front="How does AGUDLP differ from AGDLP and when do you use it?" back="AGUDLP inserts Universal groups between Global and Domain Local: Accounts to Global to Universal to Domain Local to Permissions. You use it in multi-domain forests to collect per-domain Global role groups into one forest-wide Universal bucket before granting access. Note Universal group membership replicates to the global catalog, so you nest Global groups (not individual users) into Universal groups to limit GC churn." />
<Flashcard front="What are the two main AD DS trust directions, and what does a transitive trust mean?" back="A one-way trust lets users in the trusted domain access resources in the trusting domain but not vice versa; a two-way trust grants access in both directions. Transitive means trust flows through the chain — if A trusts B and B trusts C, then A trusts C automatically. Intra-forest trusts between parent and child domains are two-way and transitive by default." />
<Flashcard front="What is a forest trust versus an external trust in AD DS?" back="A forest trust links two entire forests at the root and is transitive across all domains in both forests (every domain in forest A can trust every domain in forest B). An external trust links a single domain to a single domain in another forest and is non-transitive — it covers only those two specific domains, not the rest of either forest." />
<Flashcard front="In the Microsoft tiering model, what belongs in Tier 1 and Tier 2, and why must admin accounts never span tiers?" back="Tier 1 is member servers and application or service workloads; Tier 2 is user workstations and devices. Tier 0 is the identity control plane (DCs, FSMO holders, AD admin accounts, PAWs). Admin accounts must not be reused across tiers because using a higher-tier credential on a lower-tier machine exposes it to memory theft (LSASS dump, pass-the-hash), letting a low-tier compromise escalate upward." />
<Flashcard front="Are Organizational Units (OUs) a security boundary in AD DS?" back="No. OUs exist to delegate administration and to link Group Policy — they are containers, not security boundaries. The security boundary is the forest (and the domain is an administrative/replication boundary). Compromising rights over an OU is an admin-delegation problem, not a boundary crossing; do not treat OU placement as isolation." />
<Flashcard front="In Group Policy, how do Block Inheritance and Enforced (No Override) change the normal LSDOU precedence?" back="Block Inheritance, set on an OU, stops GPOs linked higher up (site/domain) from applying to that OU. Enforced (No Override), set on a GPO link, forces that GPO to apply and win conflicts even past a Block Inheritance — Enforced always wins. So precedence is: Enforced links beat everything, then Block Inheritance, then the normal LSDOU order (innermost OU wins)." />
<Flashcard front="What is the difference between a security group and a distribution group in AD DS?" back="A security group is a security principal — it has a SID and can be placed on an ACL to grant or deny permissions (and can also be mail-enabled). A distribution group is for email distribution only — it has no SID for authorization and cannot be used to assign resource permissions. If you need to grant access, you must use a security group." />
<Flashcard front="What is a group Managed Service Account (gMSA) and which AD DS attack does it mitigate?" back="A gMSA is a service account whose password is generated and automatically rotated by AD DS (every 30 days by default) and retrieved only by authorized hosts — no human knows or sets it. Because the password is long and random and rotates, gMSAs largely defeat Kerberoasting: even if an attacker cracks a captured service ticket, the password has rotated and the cracked value is useless." />
<Flashcard front="What is Active Directory Certificate Services (AD CS) and why is the ESC1 misconfiguration dangerous?" back="AD CS is the forest PKI that issues certificates (smart-card logon, server auth, etc.). ESC1 is a misconfigured certificate template that combines client-authentication EKU, requester-supplied Subject Alternative Name, and low enrollment rights with no manager approval — letting any permitted user request a certificate that impersonates any account, including Domain Admin. It is a certificate-based path to domain compromise (SpecterOps 'Certified Pre-Owned')." />
<Flashcard front="What is a Read-Only Domain Controller (RODC) and where is it used?" back="An RODC holds a read-only, filtered replica of AD DS — no writable directory and, by default, no cached credentials except those allowed by its Password Replication Policy. It is deployed at branch or edge sites with weak physical security so that a stolen DC exposes few or no privileged credentials and cannot be used to write changes back into the directory." />
<Flashcard front="On a forest trust, what does Selective Authentication do, and how does it differ from forest-wide authentication?" back="Forest-wide (default for some trusts) lets all users in the trusted forest authenticate to any resource. Selective Authentication denies access by default — an admin must explicitly grant the 'Allowed to authenticate' right on each specific resource a trusted-forest user may reach. It shrinks a trust's blast radius to only the resources you intend to share." />
<Flashcard front="What is SID filtering on an AD DS trust and what attack does it block?" back="SID filtering (a.k.a. quarantine) makes the trusting domain drop any SIDs in an inbound token that do not belong to the trusted domain. It blocks SID-history injection, where an attacker stamps a privileged SID (e.g. a Domain Admins SID) from the trusting domain into a token from the trusted domain to escalate across the trust. It is enabled by default on most modern trusts." />

## Check your understanding

<Quiz question={{
  id: "03-microsoft-identity/01-active-directory/q1",
  prompt: "Your PDC Emulator goes offline at 8am on a Monday. Which of the following is most likely to happen first in an active 500-user environment?",
  options: [
    "Users cannot log in at all because Kerberos stops issuing tickets entirely.",
    "Schema changes fail silently because the Schema Master is co-located with the PDC Emulator.",
    "Users who changed their password over the weekend begin seeing intermittent authentication failures and accounts lock out, because password change replication preferentially routes through the PDC Emulator.",
    "Group Policy stops applying to all computers because the PDC Emulator holds the SYSVOL master."
  ],
  correctIndex: 2,
  explanation: "The PDC Emulator processes password changes preferentially — when you change your password, the change is immediately communicated to the PDC Emulator so that if you try to log in to a DC that hasn't replicated the new hash yet, it can forward the authentication to the PDC Emulator for validation. With it offline, recent password changes cause authentication failures and lockouts until replication catches up. The PDC Emulator also owns the time master role (Kerberos failures take 5+ minutes of clock drift, not seconds) and is *involved* in GPO processing but not the sole mechanism — Group Policy continues to apply from cached data."
}} />

<Quiz question={{
  id: "03-microsoft-identity/01-active-directory/q2",
  prompt: "You are reviewing a new junior admin's proposal to use Domain Admin accounts for all server management tasks because 'they always work.' Which Microsoft hardening model does this violate, and what is the specific risk?",
  options: [
    "It violates the principle of least privilege only — the risk is accidental permission misuse.",
    "It violates the Enterprise Access Model (tiering model) by allowing Tier 0 credentials to be used at Tier 1, enabling a Tier 1 compromise to yield Tier 0 access via credential theft.",
    "It violates AD DS replication policy — Domain Admin accounts cannot replicate changes across sites.",
    "It is technically acceptable as long as the Domain Admin accounts use MFA."
  ],
  correctIndex: 1,
  explanation: "Microsoft's Enterprise Access Model (formerly the admin tiering model) exists precisely to prevent this. A Domain Admin account used on a Tier 1 server exposes those Tier 0 credentials to any attacker who can compromise that server — via memory scraping (mimikatz/LSASS dump), pass-the-hash, or pass-the-ticket. If the Tier 1 server is compromised, the attacker gets a free path to Tier 0. The entire point of tiering is to ensure that credential exposure at a lower tier cannot produce escalation to a higher tier. MFA on the account does not protect against post-auth credential theft from memory."
}} />

<Quiz question={{
  id: "03-microsoft-identity/01-active-directory/q3",
  prompt: "A security analyst shows you a BloodHound screenshot with a path: UserA → [MemberOf] → GroupB → [GenericAll] → ComputerC → [AllowedToDelegate] → DomainController01. What does this finding mean in plain English?",
  options: [
    "UserA can read all attributes of ComputerC but cannot authenticate to the domain controller.",
    "UserA, through group membership and inherited permissions, can modify ComputerC in ways that enable Kerberos delegation abuse to impersonate any user — including Domain Admins — to DomainController01, effectively yielding domain compromise.",
    "UserA is a member of a group that has read access to the domain controller's event logs, which is a low-severity finding.",
    "The path shows that ComputerC requires a domain admin to join it to the domain — a routine Active Directory operation."
  ],
  correctIndex: 1,
  explanation: "GenericAll on a computer object gives an attacker control over that computer's AD attributes, including the ability to configure Resource-Based Constrained Delegation (RBCD). RBCD lets you specify which accounts can impersonate other accounts to that resource. Combined with an existing machine account (or a new one created within the default MS-DS-MachineAccountQuota of 10), the attacker can impersonate Domain Admin to DomainController01 via S4U2Proxy, obtaining a service ticket that grants domain controller access — effectively Domain Admin. BloodHound finds these paths automatically. Every path BloodHound shows you is a real attack path, not a theoretical one."
}} />

<Quiz question={{
  id: "03-microsoft-identity/01-active-directory/q4",
  prompt: "A junior admin places a department's users directly into an Organizational Unit and tells you 'the OU isolates them as a security boundary.' Why is this statement wrong?",
  options: [
    "Because users must be placed in the domain root, never in an OU.",
    "Because an OU is a delegation-of-administration and Group Policy container, not a security boundary — the forest is the AD DS security boundary and the domain is an administrative/replication boundary.",
    "Because OUs cannot contain user objects, only computer objects.",
    "Because an OU only becomes a security boundary once a GPO is linked to it."
  ],
  correctIndex: 1,
  explanation: "OUs exist to delegate administrative control and to scope Group Policy — they are organizational containers, not isolation/security boundaries. In AD DS the forest is the true security boundary and the domain is an administrative and replication boundary. Treating an OU as isolation leads to delegation mistakes (the very pattern behind many BloodHound escalation paths)."
}} />

<Quiz question={{
  id: "03-microsoft-identity/01-active-directory/q5",
  prompt: "Your environment has dozens of service accounts with old, never-rotated passwords, and a recent assessment flagged Kerberoasting risk. Which AD DS feature most directly mitigates Kerberoasting for these service accounts?",
  options: [
    "Enabling Selective Authentication on all domain trusts.",
    "Converting the service accounts to group Managed Service Accounts (gMSAs), whose long passwords are auto-generated and rotated by AD DS (every 30 days by default).",
    "Moving the service accounts into a dedicated OU with Block Inheritance.",
    "Promoting a Read-Only Domain Controller in the data center."
  ],
  correctIndex: 1,
  explanation: "Kerberoasting works by requesting a service ticket for an SPN and cracking the service account's password hash offline. gMSAs defeat this because their passwords are long, random, machine-managed, and rotated automatically (default 30 days) — even a cracked ticket yields a password that has already changed. Selective Authentication (trusts), OUs/Block Inheritance (GPO scope), and RODCs (branch resilience) do not address the offline-cracking root cause."
}} />
